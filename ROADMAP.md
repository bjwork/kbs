# ROADMAP

## 当前阶段

v1 MVP 已跑通，进入 **14 天自用验证期**（2026-08-05 起，至 2026-08-19）。

验证标准（来自 `AI-KB-产品设计文档.md` 第八章）：
- 正向：主动想用、有推荐冲动、有「再加 XX 功能」冲动
- 负向：使用频率持续走低、觉得不如直接搜

## 已完成（v1）

- 目录结构：`raw/ notes/ wiki/ briefs/ scripts/`（wiki/briefs 预留，v2 启用）
- `CLAUDE.md` 知识库工作流章节：ingest 链接 / ingest 想法 / 问答流程 / category 与标签体系 / 脚本边界
- `scripts/build_index.py`：扫 frontmatter 生成 `INDEX.md`（按分类 + 按标签）
- `scripts/rel_score.py`：关联打分（标签重叠 ×2 + 标题公共子串 ×1，≥4 分双向写 related）；支持单篇增量和 `--all` 全量
- 端到端验证：3 篇示例笔记，2 篇相关知识库笔记正确关联（5 分），红烧肉笔记无关联（已修复 2-gram 虚词误匹配 → 改为 ≥3 字公共子串；已修复 inline 列表 `[a, b]` 解析）

## 进行中

- 14 天自用验证：日常往库里丢链接和想法，观察使用频率

## 待办

- 真实抓取一次链接，端到端走一遍 CLAUDE.md 里的 ingest 流程（当前只验证了笔记→关联→索引环节）
- 本地文件 ingest 已支持 md / txt / pdf / docx / html / 代码文件（docx 走 textutil 已验证；html 由 Claude 剥标签，textutil 转 html 中文乱码不可用）；**docx 转出的纯文本质量待真实文件验证**（测试样本是无格式单段文本）
- v2 编译层：主题页 / 实体页生成 + 往回织回环
- v2 选题流
- v3 周报流、更多抓取渠道（微信公众号已列规则未实测、X）
- 关联打分阈值 4 分的校准：攒到 20-30 篇后回看误判率
- 标签体系扩容：新主题凑满 3 篇后提议新增标签

## 阻塞

无。

## 最近验证

- 2026-08-05：`rel_score.py --all` + `build_index.py` 在 3 篇示例笔记上跑通；边界用例（单标签+多子串=4 分关联、单标签+单子串=3 分不关联、纯标题子串<4 不关联）符合预期。
