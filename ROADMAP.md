# ROADMAP

## 当前阶段

**v1.5 离线服务版一期已完成**（2026-08-06）：本地 FastAPI 服务 + PWA 前端，PC 浏览器和安卓（局域网 PWA）双端可检索、记笔记、传文件。继续 14 天自用验证（至 2026-08-19）。

验证标准（来自 `AI-KB-产品设计文档.md` 第八章）：
- 正向：主动想用、有推荐冲动、有「再加 XX 功能」冲动
- 负向：使用频率持续走低、觉得不如直接搜

## 已完成

### v1（Claude 驱动期）
- 目录结构：`raw/ notes/ wiki/ briefs/ scripts/`（wiki/briefs 预留，v2 启用）
- `CLAUDE.md` 知识库工作流章节：ingest 链接 / ingest 想法 / ingest 本地文件 / 问答流程
- `scripts/build_index.py`：扫 frontmatter 生成 `INDEX.md`
- `scripts/rel_score.py`：关联打分（标签重叠 ×2 + 标题公共子串 ×1，≥4 分双向写 related）
- MySQL 45 讲入库：45 篇 PDF + 45 篇带立场笔记，共 48 篇

### v1.5（离线服务版一期，2026-08-06）
- **raw/ 二进制与 git 脱钩**：pdf/docx/html 原件不进 git，纯文本 .md 版入库（防几千篇后仓库膨胀）
- **rel_score 增量打分**：倒排预筛候选集（O(N²)→O(k)），修重算时旧关联残留 bug；模拟 3000 篇单篇打分 21ms
- **服务端 `server/`**（FastAPI + SQLite FTS5）：
  - `server/indexer.py`：全量 upsert 重建索引，md 文件是本体、DB 可丢弃。中文检索方案 = 汉字间插空格 → unicode61 按单字切分 → 查询包双引号走短语匹配（正确性等价子串匹配，防 FTS 语法注入）
  - `server/main.py`：检索（全文 bm25 + 分类/标签/日期筛选 + 分页）、详情+related、tags、笔记 CRUD（删除移 trash/ 不真删）、上传（pdf→pdftotext+clean / docx→textutil / html→正则剥标签 / 文本类直读；raw/ 同名 409）
- **前端 `web/`**（原生 JS PWA 雏形）：列表+搜索+筛选+分页、详情+related 跳转、记笔记/编辑/删除、上传转换后预填正文让用户补立场
- 安全：文件名白名单校验（防路径穿越）、上传扩展名白名单、查询引号转义

## 进行中

- 14 天自用验证：日常往库里丢链接和想法，观察使用频率
- **手机端实测**：安卓 Chrome 访问 `http://192.168.8.173:8000` → 添加到主屏幕（真实设备验证未做）

## 待办

- 真实抓取一次链接，端到端走一遍 CLAUDE.md 里的 ingest 流程
- 真实 docx 文件（含表格/图片/多级标题）转换质量待验证（测试样本是无格式单段文本）
- **关联打分 TAG_DF_THRESHOLD=20 是绝对阈值**：库涨到几千篇时正常标签会全过阈值，需要重校（比如 2000 篇调到 ~200）
- **手机离线缓存（二期）**：按「主题包 + 收藏 + 最近访问」缓存，不全量；或接受手机离线只有 INDEX 级摘要
- **PWA 完整化（二期）**：manifest.json + service worker（注意「添加到主屏幕」需要 HTTPS 或 localhost，局域网 http 下 manifest 不生效，安卓 fallback 是普通网页书签）
- **jieba 标签推荐（二期）**：录入时从已有标签体系推荐候选
- v2 编译层：主题页/实体页（mysql45 系列是天然第一个编译对象）
- v2 选题流；v3 周报流、更多抓取渠道
- 标签体系动态调整：等第二主题入库后看复用率

## 阻塞

无。

## 最近验证

- 2026-08-06：v1.5 一期端到端跑通——检索（中文短语查询「回表」8 条 top1 正确）、新建笔记（写入+增量关联 6 条+索引同步）、编辑、删除（移 trash/）、上传（txt/docx/pdf 三种格式转换 OK，同名 409、非法扩展名 400、路径穿越 404）。
- 2026-08-06：rel_score 重构回归——48 篇全量重算与原结果指纹一致（顺带修复 04↔05 应有但缺失的关联）；模拟 3000 篇单篇打分 21ms。
- 2026-08-05：MySQL 45 讲批量入库完成。48 篇笔记关联重算后，同系列关联从「全部互连」降到只保留内容重叠的，跨系列零误连。
