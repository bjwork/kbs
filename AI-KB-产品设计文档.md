# AI 知识库产品设计文档

> 基于 07（产品思维）、08（架构思维）、09（技术思维）三篇方法论整理。文档定位：直接可执行的工程设计文档，拿到即可开干。

---

## 一、产品定位

**一句话**：给自己用的 AI Native 知识库——丢进链接 / 想法，AI 自动加工成带立场的笔记、编译成主题结论，反过来告诉你「今天能写哪篇」。

**定位模板**：我做的是一个给**我自己（内容创作者）**用的**AI Native 知识库**，跟 **Notion / Obsidian** 不一样的地方是**不止存储，AI 会把笔记编译成结论页并反向给我派选题**。

**两个心法锚点**：
- 小众赛道：只服务自己，不做商业产品，避开大厂正面竞争
- AI Native：核心价值由 AI 提供（摘要、编译、选题），拿掉 AI 产品就是个空文件夹

---

## 二、问题与目标

### 痛点（全部来自真实使用场景）

1. 素材散落各处（GitHub、X、公众号、PDF），收藏后再也找不到
2. 选题时知道「肯定存过相关资料」，但翻不出来
3. 想发散一个想法，没有助手帮我跨笔记串联
4. 已有笔记工具（Notion 类）只解决「进」和「存」，不解决「取」

### 产品目标

让「输入 → AI 加工 → 可问、可查、能派活」这条闭环跑通，达到**每天都在用**的验证标准。

---

## 三、MVP 定义（一页纸）

| 格子 | 内容 |
|---|---|
| **用户** | 我自己 |
| **问题** | 素材散落、选题时找不到、想发散没人帮、AI 完全没参与 |
| **输入** | 手动丢进去的链接（GitHub / X / 公众号 / 普通网页 / YouTube）、PDF、自己冒出的想法 |
| **处理** | Claude 读完 → 写带立场的摘要 → 自动打标签 → 更新索引 → 编译主题页 / 实体页 → 反向更新关联条目 |
| **输出** | 一个能问、能查、能告诉我「今天可以写哪篇」的本地知识库 |
| **不做什么** | 不做数据库、不做向量库、不做 UI、不做配图、不做协作、不做多端、不做音视频识别、不做微信对接（v1） |

**MVP 三件套**：一个文件夹 + 一套 Skill 提示词 + 一份索引文件。

**砍功能三刀已执行**：协作 / 多端（第一刀）；周报、配图、提醒（第二刀）；微信、语音、灵感对话（第三刀）——全部延后。

---

## 四、架构设计（进 / 存 / 取）

### 4.1 数据流总览

```
链接 / PDF ──→ raw 原文 ──→ 单源笔记（带立场）──┐
                                                ├──→ 编译层 ──→ 问答流
想法 / 灵感 ──→ 想法笔记 ────────────────────┘   （主题页/      选题流
                                                  实体页）       周报流
                                                    ↑
                                              往回织回环
                                          （新资料触发相关
                                            主题页重写 + 标矛盾）
```

### 4.2 进（输入）

| 维度 | 设计 |
|---|---|
| 来源 | 链接（GitHub / X / 公众号 / 普通网页 / YouTube）、PDF、自然语言想法 |
| 触发 | 用户手动丢入（Claude Code / Codex 会话窗口） |
| 格式 | URL、PDF 文件、自然语言文本 |

**要点**：想法也是一等输入源——否则只是个外部收藏夹，不是第二大脑。

### 4.3 存（分层存储）

按「生熟程度」分四层，**不按时间分层**：

| 层 | 目录 | 内容 | 读写性 |
|---|---|---|---|
| 原料层 | `raw/` | 抓回的原文、PDF，事实来源 | 只读，永不覆盖 |
| 笔记层 | `notes/` | 每条素材一篇带 AI 立场的原子笔记；自己的想法单独成篇 | 可写 |
| 编译层 | `wiki/` | 多篇同主题笔记合并成「主题页」；同一产品 / 人物汇总成「实体页」 | 可写，每次 ingest 回织 |
| 选题层 | `briefs/` | 从主题页产出的「这周能写哪篇」选题清单 | 可写 |

辅助文件：`INDEX.md`（自动生成的索引）、frontmatter（每篇笔记顶部的 YAML 元数据：标题 / 标签 / related 关联）。

**两条稳定性铁律**：
- 原料层只读 → AI 判断错了能翻回原文核对
- 每步处理不覆盖上一步 → 出错能回退

**可扩展性预留**：
- 编译层 v1 可先空着（fuxi 实际也是 v2 才加），但分层结构一开始就留位
- 多渠道抓取口子先留好，v1 只接 GitHub 也成立

### 4.4 取（三条输出流）

| 流 | 触发 | 形式 |
|---|---|---|
| **问答流** | 随手问 | AI 查 INDEX → 定位相关主题页 / 实体页 → 直接读那页 → 综合回答（不回翻原文） |
| **选题流** | 主动问「今天写啥」 | 基于主题页反推可写选题，输出可执行清单 |
| **周报流** | 每周一次 | 梳理「本周入库了什么」+「哪个主题该重新编译了」 |

**铁律**：架构设计先画「取」，再倒推「存」和「进」。「取」写不出 2 条以上就不许开干。

### 4.5 编译层的「往回织」

每入库一条新资料，AI 会：
1. 判断它属于哪些已有主题页 / 实体页
2. 把这些页面**重写一遍**（不是追加链接）
3. 标出新旧资料之间的矛盾点

这是知识库跟收藏夹的本质区别：收藏夹累加文件，知识库持续更新「我的当前判断」。

---

## 五、技术选型

**总纲**：优先轻量方案——能不上的组件不上、能用文件不用数据库、能本地跑不上云、能用现成工具不造轮子。

### 5.1 四原则

| 原则 | 落地 |
|---|---|
| 主流 | Python（AI 训练语料最多） |
| 本地 | 整库跑在本机，零服务器、零部署、零运维 |
| 简单 | 纯 Markdown 文件，无数据库、无向量库 |
| 大众 | 所用开源工具全部满足 star 多 + 半年内有更新 + 维护者活跃 |

### 5.2 抓取层（按渠道选专用工具，不做通用爬虫）

| 渠道 | 工具 |
|---|---|
| 普通网页 / 博客 | Claude Code 内置 WebFetch |
| 微信公众号 | Jina Reader（托管 reader API，输出干净 Markdown） |
| GitHub | `gh CLI`（官方命令行，直接读 README / issue / release） |
| YouTube | `yt-dlp`（抓字幕和元信息） |
| X / Twitter | `opencli twitter` 系列（社区现成工具，X 反爬最狠别自造） |

### 5.3 存储层

| 项 | 选择 |
|---|---|
| 主数据 | 纯 Markdown 文件（人可读、git 可 diff、AI 可直接喂） |
| 目录 | `raw/` + `notes/` + `wiki/` + `briefs/` |
| 元数据 | 每篇 frontmatter（title / tags / related） |
| 数据库 | 不用 |
| 向量库 / embedding | 不用 |

### 5.4 索引 + 编排层

| 项 | 选择 | 分工 |
|---|---|---|
| 自动索引 | Python 脚本（AI 写）扫 frontmatter 生成 INDEX.md | 机械活 |
| 关联推荐 | Python 脚本算「标签重叠 + 标题关键词重叠」打分 | 机械活 |
| 语义判断 | Claude 在长上下文里做（打标签、判断关联、编译主题页） | 语义活 |
| 工作流编排 | 写在 `CLAUDE.md` 里的规则，Claude 直接读并执行 | 语义活 |
| 运行环境 | 本地 Mac | — |

**核心分工原则**：机械归脚本、语义归 Claude。

### 5.5 关联系统（不上向量的替代方案）

机制一句话：**AI 打标签 → 脚本算重叠 → 双向写入 frontmatter 的 `related` 字段 → 查询时 AI 直接读关联跳转**。

具体：
1. Ingest 时 Claude 从预定义标签体系里给每篇笔记打 3-5 个标签（标签本身已带语义压缩）
2. 脚本两两比较新笔记与所有老笔记：标签重叠数 + 标题关键词重叠数，加权打分，≥ 4 分判为关联
3. 关联**双向**写入双方 frontmatter 的 `related` 列表
4. 优势：可解释（分数怎么算的清清楚楚）、可 git diff、零运维

容量上限判断：个人知识库（几万到十几万字）这套够用；真到 10 万篇再考虑向量库（大概率永远到不了）。

---

## 六、目录结构

```
knowledge-base/
├── CLAUDE.md          # 工作流规则（AI 读：ingest / 编译 / 选题 / 周报流程）
├── TECH-STACK.md      # 本选型清单
├── ARCHITECTURE.md    # 本架构文档（可选，并入 CLAUDE.md 也行）
├── raw/               # 原料层：原文、PDF（只读）
├── notes/             # 笔记层：带立场的原子笔记 + 想法笔记
├── wiki/              # 编译层：主题页、实体页
├── briefs/            # 选题层：选题清单
├── INDEX.md           # 自动生成的总索引
└── scripts/           # Python 脚本：索引生成、关联打分、关键词搜索
```

---

## 七、实施路线

### v1（MVP，目标：跑通闭环）

1. 建好目录结构 + `CLAUDE.md` 工作流规则
2. 支持链接 + 想法两类输入，跑通「抓取 → 摘要 → 打标签 → 写笔记」
3. 索引脚本 + 关联打分脚本跑通
4. 问答流可用
5. **验证标准：连续自用 14 天，看自己会不会扔**

### v2（编译层，目标：从「答得出」到「用得出」）

1. 上编译层：主题页 / 实体页生成 + 往回织回环
2. 选题流上线

### v3（增量渠道与主动输出）

1. 周报流
2. 接入微信公众号 / X / YouTube 等更多抓取渠道
3. 按 14 天验证中冒出的真实需求迭代（警惕顺手加功能）

**架构分版本原则**：v1 架构只服务 v1 MVP；功能狠狠做减法，架构算两条——能长大（分层、留扩展口）+ 不会塌（原料只读、可回退）。

---

## 八、验证计划

**自用产品验证逻辑**：自己用 14 天，看会不会扔。

**正向信号**（命中即继续投入）：
- 主动想去用，不需要逼自己
- 有推荐给朋友的冲动
- 产生「再加 XX 功能就完美了」的冲动

**失败信号**：
- 14 天内使用频率持续走低
- 每次用都觉得「还不如直接搜」

**验证手段**：先用「手动跑流程」方式（Claude Code + 一个链接 + 文件夹）跑通闭环，再逐步脚本化。

---

## 九、开放问题（开干前需敲定）

1. 标签体系预定义清单——需要先圈定初始标签集（建议 20-30 个起步，随用随扩）
2. 关联打分阈值 4 分是否合适——跑 20-30 篇笔记后回看调整
3. 主题页触发重写时机——每篇都回织还是攒 N 篇批量回织（v2 再定）
4. 周报流推送形式——终端输出 or 生成文件（v3 再定）
